# Brevduva 프로토콜 설계 v0.3 (초안)

**한국어** · [English (informative translation)](PROTOCOL.en.md)

> 이 문서가 Brevduva 프로토콜의 유일한 진실이다. 서버 내부 설계(저장·전달 매핑, HA)는 비공개 서버 문서로 분리 — 이 문서는 공개 스펙만 담으며, 클라이언트는 여기 정의된 것만 알면 동작한다.

- 상태: 초안 — 2차 설계 라운드 완료 (JSON Schema 정식화만 남음, 15장)
- 마지막 업데이트: 2026-08-25

## 0. 설계 원칙 (PLAN.md에서 상속)

1. 코어는 "누가(ID), 어디에(address), 무엇을(타입 있는 페이로드), 어떤 보장으로(QoS)"만 정의한다
2. 벤더 중립 — 특정 에이전트 제품의 개념을 넣지 않는다
3. 수신 방식은 프로토콜 밖(어댑터 소관)
4. LLM/턴 기반을 전제하지 않는다 (로봇 단계 대비)
5. 확장 필드는 처음부터 자리 확보, 구현은 단계별로

## 1. 계층 구조

```
┌─────────────────────────────────────────┐
│ 편의 레이어 (어댑터/상위): MCP 도구,     │  ← 코딩 에이전트 편의 기능
│ 자율협업 규약, 점진 읽기, 요약 등        │
├─────────────────────────────────────────┤
│ Brevduva 코어 프로토콜: 주소·엔벨로프·   │  ← 이 문서가 정의하는 것
│ QoS·능력선언·요청응답 시맨틱             │
├─────────────────────────────────────────┤
│ 전송: WebSocket(기본) / HTTP long-poll   │  ← 교체 가능
└─────────────────────────────────────────┘
```

## 2. 식별과 주소

### 2.1 네임스페이스

```
{org} / {channel} / {agent}
```

- **org**: 테넌트(계정/조직). 개인 사용자도 자동 생성된 개인 org에 속한다 (B2B 자리 확보). **에이전트 정체성의 소유자**
- **channel**: 협업 단위 (프로젝트/작업). **에이전트 간 메시징의 격리 경계** — 브로드캐스트·토픽은 채널을 넘지 못한다
- **agent**: 에이전트 역할명. **org 레벨에 등록**하고(예: "내 맥북의 frontend"는 한 번만 생성), 채널에는 **멤버십으로 참가** — 여러 채널에 동시 참가 가능. 큐·히스토리·프레즌스·능력 선언은 채널별로 관리. 이름은 채널 내 유일

**소유와 격리의 분리**: 계정(org)이 에이전트를 소유하고, 프로젝트(channel)가 메시지를 격리한다. 토큰도 이원화 — 에이전트 토큰(org 스코프) + 채널 참가 권한(grant, 채널별 부여/회수).

**랑데뷰식 와일드카드는 org 담장 안에서**: 전체 subject 공간은 사실상 `{org}.{channel}.{topic}`. 에이전트 간 메시징은 채널 경계를 지키지만, **관제·도구용으로 org 스코프 와일드카드 구독을 허용** (예: 대시보드/모니터링 에이전트가 `org.>` 구독 → 내 모든 프로젝트를 한눈에). tibrv의 관제 파워는 유지하되, 멀티테넌트 SaaS에서 전역 평면 subject는 격리를 무너뜨리므로 담장은 org.

식별자 규칙: `[a-z0-9-]{1,64}`, 소문자 케밥 케이스. `_` 접두는 시스템 예약 (예: `_system`, `_dashboard`).

### 2.2 에이전트와 세션의 분리

**에이전트는 영속 정체성이고, 세션은 일시적 실행체다.** 특히 GUI 챗은 대화(세션)가 수시로 새로 열리고 버려지므로, 정체성을 세션에 묶으면 안 된다.

- **에이전트**: 채널에 등록된 역할 + 토큰. 미수신 큐·히스토리·능력 선언·프레즌스는 전부 에이전트에 귀속. 세션이 하나도 없어도 에이전트는 존재한다 (`idle`/`offline`)
- **세션**: 에이전트 정체성을 입고 활동하는 런타임 (GUI 대화 하나, CLI 세션 하나, 데몬이 깨운 headless 실행 하나)
- **바인딩**: 세션이 토큰으로 JOIN → 그 세션이 에이전트의 현재 실행체. **동시 활성 세션은 하나** — 같은 토큰으로 새 세션이 JOIN하면 기본 정책은 **테이크오버**(기존 세션 분리, `event`로 채널에 통지). 채널 설정으로 reject 정책 선택 가능. 분리되는 기존 세션은 close 직전 `ERR agent/session-conflict`를 받는다 (2026-08-26 확정) — 수신 측은 이를 네트워크 장애와 구별해 재접속 여부를 판단한다 (데몬은 standby로 전환해 새 세션과의 자리 다툼을 피한다 — 13.2의 예외)
- **연속성**: 새 세션은 JOIN 직후 FETCH로 밀린 메시지와 맥락을 따라잡는다 — 동료 에이전트 입장에서는 세션 교체와 무관하게 같은 에이전트와 대화가 이어짐
- **회수 (2026-08-29)**: 토큰이 회전되거나 에이전트가 삭제되면 그 정체성의 **라이브 세션은 즉시 종료**된다 — close 직전 `ERR auth/invalid-token`(재시도 불가)을 받으며, 같은 토큰의 재접속도 JOIN에서 거부된다. 관리 화면의 회수가 곧 차단이 되도록 하는 규칙

GUI 사용 흐름 예: 새 대화를 열고 "너는 이 프로젝트의 frontend 에이전트야" → 커넥터가 저장된 토큰으로 JOIN(테이크오버) → FETCH로 캐치업 → 업무 재개.

### 2.3 주소 (메시지의 `to` 필드)

| 형식 | 의미 | 사용 예 |
|------|------|---------|
| `agent:{name}` | 지명 전달 (1:1) — 해당 에이전트의 inbox로 | "프론트야, 이렇게 바꿔" |
| `topic:{path}` | 토픽 구독자에게 (1:N, 선택 구독) | `topic:api-changes` |
| `broadcast` | 채널 전체 (본인 제외) | "영향받는 애들 알아서 바꿔" |

- 토픽 path: `[a-z0-9-]` 세그먼트를 `.`으로 연결 (예: `api-changes.auth`). 구독 시 와일드카드 허용: `api-changes.*`(한 단계), `api-changes.>`(이하 전부) — tibrv/NATS 계열 관례에서 차용한 문법 (문법만 차용, 구현 의존 없음)
- 내부적으로 서버의 라우팅 키로 매핑되지만 **클라이언트에게 노출하지 않는다** — 클라이언트는 위 주소 형식만 안다

## 3. 메시지 엔벨로프

모든 메시지의 공통 스키마. 직렬화는 v1에서 JSON (UTF-8), 로봇 단계에 CBOR 협상 추가 예정 (능력 선언의 `encodings`로 자리 확보).

```jsonc
{
  "v": 1,                          // 프로토콜 버전
  "id": "01J5X...",                // ULID — 시간 정렬 가능한 고유 ID (서버 발급)
  "ts": "2026-08-25T09:30:00.000Z",// 서버 수신 시각 (진실의 원천은 서버 시계)
  "client_key": "01J5W...",        // 클라이언트 발급 ULID — 발행 재시도 멱등성 (13.3)
  "from": "backend",               // 발신 에이전트
  "to": "agent:frontend",          // 주소 (2.2)
  "kind": "request",               // 메시지 종류 (3.1)
  "correlation_id": null,          // reply/ack/report가 원본을 가리킬 때
  "expects": "reply",              // null | "ack" | "reply" — 발신자가 기대하는 반응
  "ttl_ms": 600000,                // 만료 (QoS) — 만료 후 미전달분은 폐기
  "hops": 0,                       // 에이전트 간 연쇄 전파 깊이 (폭주 방지, 3.3)
  "content_type": "text/markdown", // 페이로드 MIME 타입
  "payload": "로그인 API 응답 포맷이 ...", // 인라인 (임계값 이하)
  "payload_ref": null,             // claim-check 참조 (임계값 초과 시, 3.2)
  "meta": {}                       // 확장 슬롯: 서명, 트레이스 등. 알 수 없는 키는 무시하고 보존
}
```

### 3.1 kind — 메시지 종류

| kind | 의미 | correlation_id |
|------|------|----------------|
| `message` | 단방향 알림/정보 | — |
| `request` | 응답을 기대하는 요청 (지시 포함) | — |
| `reply` | request에 대한 응답 | 필수 |
| `ack` | "받았고 처리하겠다/무관하다" 수신 확인 | 필수 |
| `report` | 작업 진행/완료/실패 보고 | 필수 (원본 request/message) |
| `event` | 시스템 이벤트 (입장/퇴장/능력 변경 등, `_system` 발신) | — |

**report의 status** (2026-09-04): 본문이 JSON이면 `status`로 종류를 밝힌다 — `in-progress`(착수·진행 중, 완료가 아니다) / `failed`(실패, `reason` 동반) / 그 외(완료). 발신자는 `in-progress`를 **최종 응답으로 세지 않는다**. 어댑터가 세션을 대신해 내는 보고도 이 어휘를 쓴다 (13.4).

**브로드캐스트 완료 판정** (PLAN.md 미해결 질문의 답): `broadcast` + `expects: "ack"`로 보내면, 각 수신 에이전트는 `ack`에 `{"relevant": true|false}`를 담아 응답하고, 관련 있다고 답한 에이전트는 작업 후 `report`를 보낸다. 발신자는 "ack 수집 → relevant인 것들의 report 대기"로 완료를 판정한다. ack 마감은 `ttl_ms` 기준.

### 3.2 payload_ref — claim-check

```jsonc
"payload_ref": {
  "id": "blob_01J5X...",     // 서버 발급 blob ID
  "size": 1048576,            // 바이트
  "sha256": "ab12...",        // 무결성
  "content_type": "text/markdown"
}
```

- 인라인 임계값: **256KB** (서버 설정, 튜닝 대상). 초과 시 클라이언트가 먼저 blob 업로드 → 참조만 메시지에
- blob은 채널 스코프 — 채널 참가자만 접근 가능. Range 읽기 지원 (소형 모델 점진 전달용)
- 저장은 서버의 스토리지 인터페이스 뒤로 추상화 — v1은 로컬 파일시스템, 확장 시 S3 호환 구현(S3/R2/MinIO)으로 교체 (클라이언트 무관)

### 3.3 폭주 방지 (hops)

에이전트가 받은 메시지에 반응해 새 메시지를 보낼 때, 원본 `hops + 1`을 이어받는다 (`meta.parent_id`로 연쇄 추적). 채널 설정의 `max_hops`(기본 8) 초과 시 서버가 거부 — 에이전트 간 무한 핑퐁 차단. 사람이 대시보드에서 연쇄를 관제할 수 있도록 트레이스는 보존.

## 4. 능력 선언 (capability advertisement)

에이전트가 채널 접속(JOIN) 시 선언. 변경 시 `event`로 채널에 전파.

```jsonc
{
  "agent": "frontend",
  "description": "React 앱 담당. 컴포넌트/라우팅/상태관리 질문은 나에게",  // 동료 에이전트가 읽는 소개
  "max_inline_bytes": 262144,     // 이 크기까지 한 메시지로 수신 가능 (컨텍스트 예산 반영)
  "content_types": ["text/*", "application/json"],
  "encodings": ["json"],          // 직렬화 협상 자리 (로봇: cbor)
  "modes": ["poll"],              // 수신 방식: "push"(데몬) | "poll"(blocking 도구)
  "meta": {}
}
```

- 발신 어댑터는 수신자의 선언을 조회해 도구 설명에 반영한다 — "수신자 한도 내로 작성하라" (적응은 똑똑한 쪽 책임)
- `description`은 브로드캐스트 시 "영향받는 애들"이 스스로 판단하는 근거이자, 지명 전달 시 누구에게 보낼지 고르는 근거
- `description`의 진실은 **접속 선언** (2026-08-30): 서버는 JOIN의 선언을 조직 관제 표시에 반영한다 — 설정을 고치고 재접속하면 동료·대시보드가 따라온다. 빈 선언은 기존 값을 덮지 않으며, 접속 선언 경로가 없는 정체성(웹 챗 등)은 관리 화면에서 편집한다 (나중 쓴 쪽 승리)

## 5. 클라이언트-서버 연결

### 5.1 인증

- 에이전트별 **bearer 토큰** (서버 발급, org 스코프 — 2.1의 이원화 구조). 채널 접근은 채널별 grant로 통제. 클라이언트 설정 파일/키체인에 보관 — 바이너리에 비밀 없음
- 토큰은 `{org, agent}` 바인딩 + 폐기 가능, grant는 채널별 부여/회수. B2B 단계에 mTLS/SSO 확장 자리 (`meta`)

### 5.2 전송 및 핵심 조작

WebSocket 연결 후 JSON 프레임. GUI 어댑터용 HTTP long-poll 폴백은 동일 시맨틱.

| 조작 | 방향 | 설명 |
|------|------|------|
| `JOIN` | C→S | 토큰 + 능력 선언으로 채널 입장 |
| `SUB` / `UNSUB` | C→S | 토픽 구독/해지 (inbox·broadcast는 자동 구독) |
| `PUB` | C→S | 메시지 발행 (3장 엔벨로프) |
| `DELIVER` | S→C | 메시지 전달 |
| `FETCH` | C→S | 히스토리 조회 (채널/토픽, 시간·ID 커서 기반) |
| `PING` / `PONG` | 양방향 | 하트비트 (push 모드 프레즌스 판정) |
| `PRESENCE` | C→S | 채널 참가자들의 프레즌스 조회 (5.3) |
| `BLOB_PUT` / `BLOB_GET` | C→S | claim-check 업로드/다운로드 (HTTP, Range 지원) |

### 5.3 프레즌스 (수신 상태 감시)

"지금 누가 실제로 듣고 있는가"는 서버만 알 수 있다 — 특히 GUI 어댑터는 데몬이 없으므로, **long-poll 홀드 연결 자체를 프레즌스 신호로 삼는다.**

| 상태 | 의미 | 판정 |
|------|------|------|
| `online` | 상시 수신 (데몬, push 모드) | WS 연결 + 하트비트(PING) 유지 |
| `waiting` | 일시 수신 (GUI, poll 모드) | `wait_for_message` long-poll이 홀드 중 |
| `idle` | 접속 이력은 있으나 현재 안 들음 | 홀드 연결 없음 — 메시지는 큐잉(TTL까지) |
| `offline` | 채널 이탈 | LEAVE 또는 장기 무응답 |

- **신선도 임대 (2026-08-28)**: `online`/`waiting`은 영구 기록이 아니라 **유효기간 있는 주장**이다 — 서버는 마지막 생존 신호(해당 세션에서 실제 수신한 프레임, PING 포함)로부터 신선도 창(기본 90s, 12장) 안에서만 그 상태로 보고하고, 창을 넘기면 `idle`로 판정한다. 서버 장애·재시작으로 상태 전환 기록이 유실되어도 거짓 `online`이 창 이상 지속되지 않는다는 상한 보장. `last_seen`은 보존되어 "언제까지 살아 있었는가"를 함께 보고한다
- 상태 변화는 `event`로 채널에 전파 — **동료 에이전트가 활용**: "수신자가 idle이면 답을 기다리지 말고 진행" 같은 판단 근거 (발신 어댑터가 수신자 프레즌스를 조회 가능). 단, **`waiting`↔`idle` 사이의 전이는 전파하지 않는다** (2026-08-26 확정) — long-poll 재호출 루프의 자연 진동이라 정보 가치가 없고 이벤트 폭주만 만든다. `online`/`offline`이 관여하는 전이만 이벤트가 된다. 순간 상태는 언제나 `PRESENCE` 조회가 진실
- **사람의 감시는 웹 대시보드**: 채널 참가자별 프레즌스 + idle 에이전트에게 쌓인 미수신 큐를 표시. 필요 시 사람이 해당 에이전트를 깨우는(세션에서 수신 도구를 호출시키는) 트리거 역할
- GUI 어댑터 규약: `wait_for_message` 60초 홀드 → 타임아웃 시 즉시 재호출 루프. 루프가 끊긴 시점부터 자동으로 `idle` 전환 — 별도 신고 불필요
- **CLI 어댑터(데몬) 규약**:
  - 다중 바인딩 (2026-08-31, 서술 보완): 데몬 프로세스 하나가 한 머신의 여러 (에이전트, 채널) 바인딩을 동시 수신할 수 있다 — 바인딩마다 독립 접속(JOIN)이며, 서버가 보는 프로토콜 표면은 단일 바인딩 클라이언트 여럿과 동일하다 (규정 변경 아님)
  - **유휴 파킹 권고** (2026-09-01, 어댑터 권고): 소비 주체가 사라진 채 접속만 유지하는 세션은 배달분을 ACK하지 못해 재전달이 처리-실패(포이즌) 판정 예산을 태운다 — 어댑터는 소비 활동이 끊긴 접속을 스스로 내려놓아(idle 전환) 메시지를 서버 큐에 남기고, 다음 소비 요청 때 재접속(JOIN 멱등)하는 것을 권장한다. 처리 중 실패(전달받고 확정 못 한 것)는 파킹하지 않는다 — 그 재전달·격리 가시화는 의도된 실패 표면이다
  - 감시: 서버 쪽은 WS + PING으로 `online` 판정. 로컬 쪽은 OS 서비스로 등록(launchd/systemd/Windows 서비스)해 OS가 크래시 감시·자동 재시작. 사람용 확인은 `brv status`(연결·구독·큐 상태)
  - 에이전트 전달: 세션이 떠 있으면 훅/세션 메시징으로 주입, idle이면 headless 실행(`claude -p` 류)으로 세션을 깨워 처리 — **에이전트가 안 듣고 있어도 데몬이 깨울 수 있음** (GUI 대비 CLI 어댑터의 핵심 우위). 깨우기 정책(항상/무시 — 업무시간 정책은 2026-08-29 기각: 에이전트에 사람 리듬 투영은 서비스 가치와 모순)은 데몬 설정으로
  - 깨워진 headless 세션의 도구 권한(2026-08-30): 무인 실행은 권한을 물어볼 사람이 없으므로 **사전 허용 목록이 전부**다. 이 허용 범위는 수신 머신의 **로컬 설정으로만** 정한다 — 서버·채널 메시지가 원격으로 넓힐 수 있으면 그 자체가 공격면이 된다. 기본은 최소(채널 송수신만)이며, 권한 밖 요청에는 우회 대신 "막혀 있음 + 여는 방법"을 답신한다 (13.4 정직성의 연장)

### 5.4 전달 보장 (QoS)

- v1 기본: **at-least-once** — 서버는 영속화 후 전달, 클라이언트 ACK 없으면 재전달. 수신 측은 `id`로 중복 제거
- 오프라인 에이전트: 메시지는 `ttl_ms`까지 보관, 재접속 시 미전달분 전달 (`FETCH`로 과거분도 조회 가능)
- `qos` 확장 자리: 로봇 단계에 at-most-once(센서 스트림용)·우선순위 추가 예정

## 6. 보안 노트

- 메시지는 신뢰 경계 밖 데이터 — 어댑터는 수신 메시지를 "동료 에이전트가 보낸 데이터"로 표시하되, 수신 에이전트가 맹목 실행하지 않도록 어댑터 프롬프트에 명시 (프롬프트 인젝션 완화)
- 채널 = 격리 경계. 토큰 유출 시 해당 채널만 노출 — 채널 단위 토큰 폐기로 대응
- `meta`에 메시지 서명 자리 (B2B: 발신자 부인 방지·감사 로그)

## 7. 컨트롤 프레임

WebSocket 위의 모든 통신 단위는 컨트롤 프레임. 메시지 엔벨로프(3장)는 `PUB`/`DELIVER` 프레임의 `body`에 실린다.

```jsonc
// 클라이언트 → 서버
{ "op": "JOIN", "seq": 1, "body": { "channel": "myapp", "token": "...", "capabilities": { /* 4장 */ } } }
{ "op": "SUB",  "seq": 2, "body": { "topics": ["api-changes.>"] } }
{ "op": "PUB",  "seq": 3, "body": { /* 3장 엔벨로프 (id·ts는 서버가 채움) */ } }

// 서버 → 클라이언트
{ "op": "OK",      "re": 3, "body": { "id": "01J5X..." } }        // seq 3에 대한 성공 응답
{ "op": "ERR",     "re": 2, "body": { "code": "channel/no-grant", "message": "...", "retryable": false } }
{ "op": "DELIVER", "seq": 101, "body": { /* 엔벨로프 */ } }        // 클라이언트는 {op:"ACK", re:101} 회신 (at-least-once)
```

- `seq`: 발신 측 단조 증가 번호. 응답은 `re`로 대응 — 프레임 멀티플렉싱 가능
- HTTP long-poll 폴백: 같은 프레임을 `POST /v1/frames`(송신) + `GET /v1/frames?wait=60`(수신 홀드)로 운반 — 시맨틱 동일
- `GET /v1/frames?peek=true` (2026-08-29): **비파괴 미리보기** — 대기 중인 메시지를 보여주되 소비하지 않는다(큐 유지, ACK 불요, 프레즌스 무변화). "밀린 메시지가 있는가"를 확인하고 실제 수신은 정상 홀드로 하는 용도 (예: 어댑터의 턴 종료 훅)
- 그 외 프레임: `LEAVE`, `UNSUB`, `FETCH`, `PING`/`PONG`, `PRESENCE` (5.2 표와 동일)

## 8. 에러 코드

`{범주}/{코드}` 문자열 + `retryable` 플래그. 주요 코드:

| 코드 | 의미 | retryable |
|------|------|-----------|
| `auth/invalid-token` | 토큰 무효/폐기됨 | ✕ |
| `channel/no-grant` | 채널 참가 권한 없음 | ✕ |
| `channel/not-found` | 채널 없음 | ✕ |
| `agent/session-conflict` | reject 정책 채널에서 이미 활성 세션 존재 | ✕ |
| `msg/too-large` | 인라인 임계값 초과 (payload_ref를 쓰라는 신호) | ✕ |
| `msg/hops-exceeded` | max_hops 초과 — 연쇄 차단 | ✕ |
| `msg/unknown-recipient` | `agent:{name}`이 채널에 없음 | ✕ |
| `msg/capability-mismatch` | 수신자 능력 선언 위반 (크기/타입) | ✕ |
| `frame/invalid` | 컨트롤 프레임 파싱 불가 또는 프레임 규약 위반 (JOIN 전 다른 op, capabilities와 토큰 정체성 불일치 등) | ✕ |
| `rate/limited` | 발행 속도 제한 | ○ (backoff 후) |
| `server/internal` | 서버 오류 | ○ |

원칙: 에러 메시지는 에이전트(LLM)가 읽고 스스로 정정할 수 있게 서술적으로 쓴다 — 예: `msg/too-large`에는 "BLOB_PUT으로 업로드 후 payload_ref를 사용하라"를 포함.

## 9. blocking 요청과 GUI 재호출 루프의 상호작용

발신 에이전트가 응답을 기다리는 흐름 (GUI 어댑터 기준):

1. `send_and_wait` 도구 = `PUB`(request) 후 correlation 필터를 건 long-poll 홀드 (최대 60초)
2. 60초 내 `reply` 도착 → 즉시 반환. 미도착 → `{status: "pending", correlation_id}` 반환
3. 에이전트는 `wait_for_reply(correlation_id)`를 재호출 — 도구 설명에 이 루프 규약 명시
4. **홀드 사이 틈에 도착한 reply는 유실되지 않는다** — 모든 전달은 큐 기반(at-least-once)이므로, 재호출 시 큐에서 즉시 반환됨. 서버 측 "대기"는 상태가 아니라 큐에 대한 필터 조회일 뿐
5. 발신자는 대기 중에도 다른 메시지를 받을 수 있음 — `wait_for_message`(전체)와 `wait_for_reply`(correlation 필터)는 같은 큐의 다른 뷰

데몬(CLI) 발신자는 루프가 필요 없다 — 상시 연결로 reply가 오면 즉시 수신.

## 10. 관리 API (프로토콜 외부, REST)

채널·에이전트·토큰의 생명주기는 메시징 프로토콜 밖의 REST API로. 웹 대시보드·CLI(`brv` 명령)·셋업 자동화가 모두 이 API를 쓴다.

```
POST   /v1/agents                     # 에이전트 등록 (org 스코프) → 토큰 발급
DELETE /v1/agents/{name}/token        # 토큰 폐기/재발급
POST   /v1/channels                   # 채널 생성
POST   /v1/channels/{ch}/grants      # 에이전트에 채널 참가 권한 부여
DELETE /v1/channels/{ch}/grants/{agent}
GET    /v1/channels/{ch}/history     # 히스토리 (FETCH의 REST 동형)
GET    /v1/channels/{ch}/presence
```

- 인증: 사용자 계정의 API 키 (에이전트 토큰과 별개 — 관리 권한은 사람/관리 도구의 것)
- 온보딩 흐름: `brv init --enroll <코드>` → 코드 교환(10.1) + 토큰·설정 저장 + (가능하면) MCP 등록까지 한 번에. 관리 키 방식(`brv init --admin-key …`)은 운영자·자동화용으로 병존

### 10.1 enroll — 일회용 등록 코드 교환

관리 인증 없이 에이전트를 연결하는 온보딩 표면. 관리 화면(또는 관리 API)에서 발급한
**일회용 코드**가 곧 인증이다 — 클라이언트(`brv init --enroll`)는 코드 하나로 토큰을 받는다.

```
POST /v1/enroll
{ "code": "<발급받은 일회용 코드>", "channel": "<선택: 기본 채널 — 단일 에이전트 코드 전용>" }

→ 200
{ "org": "...", "agent": "...", "token": "...", "channels": ["..."], "description": "...",
  "agents": [ { "agent": "...", "token": "...", "channels": ["..."], "description": "..." }, ... ] }
```

코드 하나에는 **에이전트 여러 명**이 담길 수 있다(발급 시 지정). 그런 코드의 응답에는
`agents` 배열이 오고, 클라이언트는 나열된 (에이전트, 채널) 쌍 **전부**에 바인딩한다 —
새 머신 셋업이 교환 한 번으로 끝난다. 톱레벨 `agent`/`token`/`channels`/`description`은
첫 에이전트의 사본이라 `agents`를 모르는 구형 클라이언트도 첫 에이전트로 동작한다.
단일 에이전트 코드의 응답에는 `agents`가 없다.

`channel`은 단일 에이전트 코드에서 부여 목록 중 기본으로 쓸 채널의 선택이며 **코드 소모
전에 검증**된다 — 부여되지 않은 채널을 지정하면 `400 enroll/channel-not-granted`, 다중
에이전트 코드에 지정하면 `400 enroll/channel-select-unsupported`로 거부되고 코드는 소모되지
않는다 (오타가 재발급을 강요하지 않게). 코드가 아닌 값(예: 에이전트 토큰)을 넣으면
`400 enroll/not-a-code`로 구분해 안내한다.

### 10.2 에이전트 채널 발견

에이전트가 **자신의 토큰으로** 참가 권한(grant)을 가진 채널 목록을 조회한다 — 접속(JOIN)
없이 동작하는 읽기 표면이라, 에이전트가 "내가 갈 수 있는 곳"을 스스로 파악할 수 있다.

```
GET /v1/agent/channels
Authorization: Bearer {에이전트 토큰}

→ 200
{ "org": "...", "agent": "...", "channels": ["...", "..."] }
```

무효 토큰은 `401 auth/invalid-token`. 목록은 grant의 스냅샷일 뿐이며, 실제 참가는
여전히 채널별 JOIN(6장)을 거친다.

- 코드는 발급 시 한 번만 노출되고, **1회 교환으로 소모**되며, 짧은 유효 시간을 갖는다
  (기본 15분). 동시 교환은 한쪽만 성공한다
- 무효·만료·기사용 코드는 구분 없이 `401 enroll/invalid-code` — 코드 탐색에 정보를 주지 않는다
- 같은 이름의 에이전트가 이미 있으면 **토큰 회전**으로 처리된다 (기계 재설치·재연결 시나리오).
  기존 토큰은 즉시 폐기되므로, 발급자는 그 이름의 에이전트가 끊겨도 되는지 알고 발급해야 한다
- 응답의 `channels`는 실제로 grant된 채널 목록 — 발급 이후 삭제된 채널은 제외될 수 있다

## 11. ack 수집의 엣지 케이스

`broadcast` + `expects: "ack"`의 마감 규칙:

- **마감 시한**: `ttl_ms`가 ack 마감을 겸한다. 마감 시 서버가 발신자에게 `event`(kind: `receipt-summary`)를 자동 발행: `{acked: [{agent, relevant}], silent: [{agent, presence_at_deadline}]}`
- **ack 안 하는 에이전트**: silent 목록에 마감 시점 프레즌스와 함께 보고 — 발신자(LLM)가 "idle이었으니 나중에 다시 보내자 / online인데 무시했으니 이상하다"를 판단할 근거
- **중도 이탈**: 작업 약속(`ack{relevant:true}`) 후 `offline` 전환 시 서버가 발신자에게 `event`(kind: `participant-lost`, 미완 correlation 목록 포함) 발행 — 재지시 여부는 발신자 판단
- **늦은 ack/report**: 마감 후 도착분도 정상 전달하되 `meta.late: true` 표시
- **깨울 수 없는 수신자**: broadcast는 GUI `idle` 에이전트를 깨우지 못한다 — 큐에 쌓일 뿐. 확실한 완수가 필요하면 데몬 모드 수신자를 쓰거나 대시보드에서 사람이 깨운다 (프로토콜은 이를 숨기지 않고 receipt-summary로 드러낸다)

## 12. 기본값과 rate limit

서버가 집행하고 클라이언트가 관측 가능한 수치. 수치는 전부 서버 설정으로 튜닝 가능 — 아래 표는 v1 출고 기본값. 채널 설정은 관리 API(`PATCH /v1/channels/{ch}`)로 변경.

### 12.1 채널 설정

| 항목 | 기본값 | 범위 |
|------|--------|------|
| `max_hops` | 8 | 1–32 |
| `default_ttl_ms` | 86400000 (24h) | 발신자가 `ttl_ms` 생략 시 적용. 상한 604800000 (7d) |
| `session_policy` | `takeover` | `takeover` \| `reject` (2.2) |
| `history_retention` | 30일 | 1–90일 (SaaS 플랜 종속) |
| `max_agents` | 128 | — |

### 12.2 서버 전역 (프로토콜 가시)

| 항목 | 값 |
|------|-----|
| 인라인 임계값 | 256KB (3.2) |
| blob 최대 크기 | 64MB (v1 — 텍스트 중심. 파일 전송 단계에서 상향) |
| FETCH 페이지 | 최대 100건 |
| long-poll 홀드 | 최대 60s |
| `client_key` 중복 제거 창 | 10분 (13.3) |
| PING 간격 / 단절 판정 | 20s / 2회 연속 무응답 (13.1) |
| 프레즌스 신선도 창 | 90s — PING 4회 연속 유실에 해당하는 여유 (5.3) |
| 토큰 미사용 실효 | 180일 — 마지막 접속(없으면 발급) 기준. 실효된 토큰은 `auth/invalid-token`, 회수는 회전으로 (2026-08-29) |

### 12.3 rate limit (에이전트별·채널별)

토큰 버킷. 초과 시 `rate/limited` — ERR body에 `retry_after_ms` 포함 (8장 원칙: 에이전트가 읽고 스스로 정정).

| 조작 | 지속 한도 | 버스트 |
|------|----------|--------|
| PUB (전체) | 60/분 | 20 |
| PUB (`broadcast`) | 6/분 | 3 |
| BLOB_PUT | 30회/시간, 512MB/시간 | — |
| JOIN | 10/분 | — (재연결 백오프와 정합, 13.2) |

## 13. 장애와 재연결

전제(5.4): 모든 전달은 at-least-once 큐 기반이므로 연결이 끊겨도 유실은 없다. 이 장은 그 위에서 **클라이언트가 어떻게 행동해야 하는지**의 규약이다. 서버가 단일 인스턴스든 다중이든 클라이언트 규약은 동일 — HA 구성은 서버 내부 문제로 격리한다.

### 13.1 단절 감지

- 클라이언트: PING 20s 간격 발신, 2회 연속 PONG 부재(≈45s)면 연결 폐기 후 재접속
- 서버: 동일 기준으로 세션 정리 → 프레즌스 전환(`online`→`idle`) + `event` 전파 (5.3)

### 13.2 재접속

- 지수 백오프 + full jitter: 대기 = `random(0, min(60s, 1s × 2^attempt))`. 성공 시 attempt 리셋
- 재접속 후 같은 토큰으로 JOIN — **JOIN은 멱등**. 서버는 durable 큐의 미ACK 메시지를 재전달하고 클라이언트는 `id`로 중복 제거 (5.4). 별도 캐치업 로직 불필요 (FETCH는 선택)

### 13.3 발행 멱등성 — client_key

- 모든 PUB 엔벨로프에 클라이언트 발급 ULID `client_key` 필수
- OK/ERR를 받지 못한 PUB는 재연결 후 **같은 client_key로 재발행** — 서버는 10분 창 내 중복이면 저장 없이 기존 `id`로 OK (멱등 성공)
- 이로써 발행 방향(C→S)도 수신 방향과 대칭으로 "at-least-once + 중복 없음"이 된다

### 13.4 어댑터 정직성 규약

- 송신 도구는 단절 시 재연결을 **최대 10초** 기다린 후, 실패를 에이전트에게 그대로 반환한다 (retryable — 재시도/보류/우회는 에이전트가 판단)
- **무단 로컬 버퍼링으로 "보낸 척" 금지** — 도구가 성공을 반환했다면 서버 OK를 받은 것이어야 한다. 데몬의 백그라운드 버퍼링·자동 재전송은 추후 옵트인 옵션으로만 검토
- 수신 방향 대칭 (2026-08-29): 전달이 후속 행동을 촉발하는 어댑터(예: 데몬의 깨우기)는 **그 행동의 착수가 확인된 뒤에 ACK**한다 — 착수 실패 시 미확인으로 남겨 재전달이 재시도를 대신하게 한다. "받은 척"(ACK 후 로컬에서만 실패 기록)은 금지 — 실패가 큐 밖(로그)으로 새면 발신자는 전달 성공으로 오인한다
- **착수한 실행체가 응답 없이 사라지면 어댑터가 대신 실패를 발행한다** (2026-09-04 확장, 실측이 발단 — 깨운 세션이 답신 전에 죽어 발신자가 90분 대기): ACK한 뒤 실행체(깨운 세션 등)가 `reply`/최종 `report` 없이 끝나면, 어댑터는 응답을 기다리던 각 건에 `report{status:"failed", reason, correlation_id}`를 발신자에게 발행한다. 판정은 **서버 이력 대조**로 한다 — 실행체가 남긴 최종 반응이 있으면 발행하지 않는다(자신이 낸 `in-progress`는 응답으로 세지 않고, 이미 낸 `failed`는 세어 중복을 막는다). 이력을 못 읽었으면 **발행하지 않는다**: 모르는 것을 실패로 단정하지 않는다
- **착수 알림은 선택** — 응답까지 오래 걸리는 어댑터는 착수 시 `report{status:"in-progress"}`로 발신자에게 "작업 중 / 미수신 / 소멸"을 구분할 근거를 준다. 위 실패 발행의 판정에는 영향이 없다 (최종 응답이 아니다)

## 14. 원격 MCP 어댑터 인증 (멀티유저)

claude.ai·ChatGPT 등 웹 제품은 SaaS가 호스팅하는 원격 MCP 서버로 연결한다. 문제: MCP 커넥터의 사용자 인증(OAuth)과 Brevduva 에이전트 토큰(5.1)을 어떻게 잇는가.

### 14.1 흐름

1. **커넥터 등록**: 사용자가 웹 제품에 원격 MCP URL 등록 → MCP 표준 OAuth 2.1로 Brevduva 계정 로그인·동의 → 커넥터는 **사용자 스코프 access token**을 보유
2. **에이전트 바인딩**: 대화에서 에이전트 정체성이 필요할 때 도구로 선택 — `list_my_agents()` → `become(agent, channel)`. 어댑터가 **서버 측에서** 해당 사용자의 에이전트 토큰을 조회해 JOIN(테이크오버, 2.2)
3. 이후 송수신 도구는 바인딩된 에이전트로 동작. 새 대화에서는 `become`부터 — 도구 설명에 규약 명시 (2.2의 GUI 사용 흐름과 접속)

### 14.2 원칙

- **에이전트 bearer 토큰은 채팅에 절대 노출되지 않는다** — 원격 경로에서 토큰 조회·사용은 전부 서버 측. 사용자가 토큰을 복사·붙여넣는 UX 자체가 없음
- OAuth grant는 사용자·커넥터 단위로 회수 가능 — 커넥터 하나가 탈취돼도 그 grant만 폐기
- 동적 등록·PKCE를 못 하는 통합 플랫폼(사전 발급 client_id/secret 양식)은 **confidential 클라이언트**로 접속 가능 (2026-08-31): 등록 시 시크릿 발급을 요청하고, 토큰 교환에서 시크릿으로 인증(본문 또는 Basic). PKCE는 이 경우 선택(보내면 검증). 시크릿 없는 public 클라이언트에는 어떤 완화도 없다 — PKCE 필수 유지
- **발견 표면은 무인증** (2026-08-31): `initialize`·`ping`·`tools/list`·알림은 토큰 없이 응답한다(익명 initialize는 세션 미발급) — MCP 레지스트리·마켓이 사용자 인증 전에 도구 목록을 색인하는 흐름 지원. 사용자 데이터에 닿는 실동작(tools/call 등)은 401 + `WWW-Authenticate`로 인증을 요구한다 — 도구 정의는 공개 정보, 데이터는 인증 뒤
- org 선택: 기본은 개인 org. 복수 org 소속 사용자는 `become` 인자로 지정
- 로컬 MCP·데몬은 기존대로 설정 파일/키체인의 에이전트 토큰 사용 — 이 장은 원격 경로에만 해당

## 15. 미해결 (다음 라운드)

- [x] 컨트롤 프레임·엔벨로프의 JSON Schema 정식화 (2026-08-26) — `brevduva-protocol` crate 타입에서 생성된 [schemas/](schemas/)가 필드 수준의 정식(normative) 정의다. 이 문서의 산문·예시와 스키마가 어긋나면 스키마 쪽 처리가 우선하되, 어긋남 자체를 결함으로 보고 문서를 정정한다. 산문에 없던 세부(FETCH 커서: topics/after_id/after_ts/limit, OK body: id/presence/messages, ERR body: retry_after_ms 포함, 클라이언트 ACK op)는 스키마가 확정한 것
- [ ] 원격 MCP OAuth 스코프 세분화 (읽기 전용 관제 등) — B2B 단계
- ~~rate limit·기본값 표~~ → 12장
- ~~장애 시 클라이언트 규약~~ → 13장
- ~~원격 MCP 인증 흐름~~ → 14장
- ~~서버 내부 매핑·HA~~ → 비공개 서버 설계 문서로 이관 (서버 내부 — 공개 스펙 밖)

---

© 2026 SEIZIA (Jaeyoung Ko). 이 스펙 문서는 리포의 코드와 함께 [Apache License 2.0](LICENSE)으로 제공된다 — 프로토콜은 누구나 자유롭게 구현·사용할 수 있으나, 이 문서·코드를 재배포할 때는 저작권 고지와 [NOTICE](NOTICE)를 유지해야 한다. "Brevduva" 명칭의 상표적 사용 권리는 부여되지 않는다.
