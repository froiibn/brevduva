# 발행·수신 확인·완료의 경계

Copyright 2026 SEIZIA (Jaeyoung Ko)

2026-09-09. 규범은 PROTOCOL.md 9·11·13·14장이며, 이 문서는 이번 수정의 적용과 검증 범위를 설명한다.

## 성공과 불확실성

발행 성공은 원본 메시지의 영속화 확인을 요구한다. 예약된 ID만으로 성공을 반환하지 않는다.
응답 유실 뒤 같은 client_key로 재시도하면 저장 원본을 확인해 같은 ID로 복구한다.
저장 여부를 확인할 수 없으면 재시도 가능한 오류로 알린다. 불확실성을 새 ID 발행으로 덮지 않는다.
중복 제거 창 이후에는 기존과 같이 새 발행이 될 수 있으므로, 오류를 받은 클라이언트가 새 client_key를 만들어 즉시 재발행해서는 안 된다.

## 수신 확인과 업무 완료

| 응답 | expects:reply의 최종 답 | expects:ack의 수신 확인 | 수락한 업무의 완료 |
|---|---|---|---|
| ack | 아니오 | 예 | 아니오 |
| in-progress 또는 상태 없는 report | 아니오 | 아니오 | 아니오 |
| reply / 최종 report | 예 | 예 | 예 |

로컬·원격 요청 대기는 ACK를 소비하되 최종 답을 계속 기다린다.
데몬은 원본의 expects를 기준으로 미응답을 판정한다. 요청에 ACK만 남기고 실행체가 종료되면 실패 보고 대상이다.
진행 보고나 발행 거부는 수락한 업무의 완료를 만들지 않는다. 수신 확인·완료 기록은 해당 메시지의 영속화 이후에만 반영한다.
공유 프로토콜 crate의 판정 함수로 어댑터들의 의미를 통일한다. 와이어 필드 변경은 없다.

## 권한 회수

원격 MCP는 바인딩된 도구마다 현재 조직 소속과 채널 grant를 검사한다.
대기 수신도 소비·반환 직전에 검사한다. 회수 시 바인딩과 수신 소유권을 해제하고 권한 오류를 반환한다.
이미 가져왔지만 반환하지 않은 메시지는 소비 확정하지 않고, 권한 있는 수신자에게 재전달되도록 돌려놓는다.
웹 대화의 자동 주입 지원 여부와 이 인가 규칙은 별개다.

## 검증 범위

회귀는 캐시 유지 중 권한 회수, 대기 중 회수와 메시지 보존, 저장 전후 중단과 동일 키 복구,
ACK→최종 답, ACK→실행체 종료, 진행 보고→이탈, 발행 거부와 정상 완료를 검증한다.
호스트 앱의 자동 주입 실기는 별도 사용자 환경에서 확인한다.


## 2026-09-09 QA 후속 수정 — 첨부 완료 판정과 테스트 환경

- report의 완료/진행 판정은 전체 원문에 공통 프로토콜 함수를 적용한다. 첨부의 status가 16KB 미리보기 밖에 있어도 인라인과 같다. 새 필드나 별도 완료 상태 저장소를 만들지 않으며 기존 첨부에도 적용한다.
- 로컬 응답 대기는 판정 후 소비를 확인한다. 첨부 읽기 오류/시간 초과는 명시적 오류이며 확인하지 않은 메시지는 재전달 대상이다. 데몬은 원문 확인 실패를 미응답으로 단정하지 않는다.
- 원격 응답 대기도 표시용 미리보기와 업무 완료 판정을 분리한다. 서버 완료 기록은 실제 영속화 뒤 전체 원문 판정에 성공한 경우만 반영한다.
- 유인/무인 보호는 운영 코드에서 매번 실제 환경으로 판단한다. 단위 테스트에서만 인스턴스별 판정 함수를 지정하여 실행 세션의 환경변수·상태 파일을 변경하지 않는다. 무인 바인딩과 진행 중 깨우기 각각에 대한 거부 회귀도 유지한다.
- 회귀 범위: 큰 done/failed/in-progress 보고의 실제 첨부 송수신, 원격 대기의 미리보기 밖 status, 큰 보고 후 실행체 종료 시 거짓 실패 부재, 첨부 읽기 실패와 복구, 무인 표식이 설정된 전체 공개 테스트.
- 실제 사용자가 설치한 CLI 세션 자동 주입 검증은 이 자동 회귀와 별개이다.

검증 결과: Windows GNU에서 fmt/clippy 통과, 공개 회귀 122 PASS. 무인 실행 표식을 둔 실행에서도 동일하게 통과했다. CI에 무인 환경 회귀 단계를 추가했다. 서버의 실제 통합 검증도 별도 완료했으며, 상세 결과는 비공개 구현 기록에 둔다. 이 수정은 v0.6.39로 배포했다. 실제 사용자 CLI 세션 자동 주입 실기는 배포 후 별도로 진행한다.
